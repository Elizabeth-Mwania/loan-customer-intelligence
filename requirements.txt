# The core local pipeline uses the Python standard library.
# These packages support tests plus optional BigQuery/dbt/Kafka/Airflow workflows.
pytest>=8.0
google-cloud-bigquery>=3.0
dbt-bigquery>=1.8
kafka-python>=2.0
